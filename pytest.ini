[pytest]
# The wizard UI bridge moved from tableio_cfg_json to wizard_ui_bridge.
# tableio_cfg_json still re-exports the moved names with this warning, so
# turning it into an error keeps an old import from creeping back in.
filterwarnings =
    error::tableio_cfg_json.WizardUiBridgeMoved
